# Backend Environment Variables

# Firebase project configuration (auto-configured when deploying to Firebase)
# For local development with emulators, these are not required

# If using a service account key for local development:
# GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# Optional: Firebase project ID
# FIREBASE_PROJECT_ID=your-project-id
